// Konfigurasi sambungan backend (Fase 4).
// Isi API_URL dengan URL Web App Apps Script SETELAH deploy (lihat apps-script/Code.gs).
// Kosong ("") = app memakai data seed statis (data/seed-esg.json) — mode review/offline.
export const API_URL = "";
